package internal

import org.specs2.mutable._
import org.junit.runner.RunWith
import org.specs2.runner.JUnitRunner

@RunWith(classOf[JUnitRunner])
class InternalTest  extends Specification {
  
  "Tests" should {
    "compile" in {
      true 
    }
  }

}